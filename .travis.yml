language: node_js
node_js:
- "node"

env:
  VERSION=1.0
    
deploy:
  provider: script
  script: "cp .npmrc.template $HOME/.npmrc && npm publish"
  skip_cleanup: true
  on:
    tags: true

notifications:
  email:
    recipients:
    - rchowson@yahoo.com
    on_success: change
    on_failure: always